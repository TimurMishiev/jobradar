{
  "name": "signalhire",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:api": "pnpm --filter @signalhire/api dev",
    "dev:web": "pnpm --filter @signalhire/web dev",
    "build": "pnpm --filter @signalhire/shared build && pnpm --filter @signalhire/api build && pnpm --filter @signalhire/web build",
    "db:migrate": "pnpm --filter @signalhire/api db:migrate",
    "db:generate": "pnpm --filter @signalhire/api db:generate",
    "db:studio": "pnpm --filter @signalhire/api db:studio"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@prisma/client", "@prisma/engines", "esbuild", "prisma"]
  }
}
